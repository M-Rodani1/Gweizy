[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r backend/requirements.txt"
]

[start]
cmd = "gunicorn backend.app:app --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120"

[variables]
PYTHONPATH = "/app"
